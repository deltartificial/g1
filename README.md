# G1

Simulate, train and evaluate Unitree G1 humanoid robot using Genesis physics engine on Apple Silicon (Metal).

## Setup

```bash
pip install -r walk/requirements.txt
```

## Usage

```bash
cd walk
python g1_train.py   # Train
python g1_eval.py    # Evaluate
```
